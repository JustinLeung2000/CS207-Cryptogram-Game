import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.HashMap;

import static org.junit.jupiter.api.Assertions.*;

class UnitTest {

    @BeforeEach
    void setUp() {
//        ArrayList<Player> playerList = new ArrayList<Player>();
//        Player p = new Player("Bob",0,0,0,0);
//        playerList.add(p);
//        Players ps = new Players(playerList,"");
//        Cryptogram cryp = new LetterCryptogram("Hello", "hello");
//        HashMap<Player, Cryptogram> playerMap = new HashMap<Player, Cryptogram>();
//        playerMap.put(p, cryp);
//        Game g = new Game(playerMap, p);
//        g.generateCryptogram();
    }

    @Test

    @AfterEach
    void tearDown() {
    }


}